/**
 * Azure Entra JWT attachment is handled by MsalInterceptor (class-based),
 * registered in app.config.ts via HTTP_INTERCEPTORS + withInterceptorsFromDi().
 *
 * This file is a placeholder to document that responsibility.
 * The actual interceptor is MsalInterceptor from @azure/msal-angular.
 */
export {};
