import { AppRole, ALL_ROLES, WRITE_ROLES } from '../../core/auth/auth-state';

export const RECONCILIATION_VIEW_ROLES: AppRole[] = ALL_ROLES;

export const RECONCILIATION_REVIEW_ROLES: AppRole[] = WRITE_ROLES;
