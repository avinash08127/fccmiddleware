package com.fccmiddleware.edge.config

/**
 * ConfigManager — manages runtime configuration received from cloud.
 *
 * Applies configuration at runtime (hot-reload where possible).
 * Persists last-known config locally so the agent functions offline.
 *
 * Stub — implementation follows EA-1.x tasks.
 */
class ConfigManager {
    // TODO (EA-1.x): load config from EncryptedSharedPreferences
    // TODO (EA-1.x): apply cloud-pushed config updates at runtime
}
