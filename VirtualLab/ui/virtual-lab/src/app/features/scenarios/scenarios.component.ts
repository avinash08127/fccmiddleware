import { Component } from '@angular/core';

@Component({
  selector: 'vl-scenarios',
  standalone: true,
  template: `
    <h2>Scenarios</h2>
    <p>
      Scenario replay remains a first-class concern. Phase 0 defines the deterministic seed and
      replay signature rules this view will surface later.
    </p>
  `,
})
export class ScenariosComponent {}
